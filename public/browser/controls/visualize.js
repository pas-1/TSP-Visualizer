const visualizeAlgo = document.getElementById('start-btn')
const dfsBtn = document.getElementById('dfs-btn')
const lexicoBtn = document.getElementById('lexico-btn')
const twooptBtn = document.getElementById('2opt-btn')
const geneticBtn = document.getElementById('genetic-btn')

let numAlgo = []
let workingContainer = []

function runVisual() {
   switch (numAlgo) {
      case 0:
         break
      case 1:
         break
      case 2:
         break
      case 3:
         break
      default:
         alert('You must select an Algorithm first!')
         break
   }
}

// Event listeners
//dfsBtn.addEventListener('click', runVisual)
//lexicoBtn.addEventListener('click', runVisual)
//twooptBtn.addEventListener('click', runVisual)
//geneticBtn.addEventListener('click', runVisual)
